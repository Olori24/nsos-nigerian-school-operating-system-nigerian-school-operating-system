import { trpc } from "@/lib/trpc";
import { BadgeCheck } from "lucide-react";
import { FormEvent, useState } from "react";
import { toast } from "sonner";

const inputClass = "h-10 w-full rounded-lg border border-[#dfe5df] bg-[#fbfcfa] px-3 text-sm text-[#15201c] outline-none transition focus:border-[#0f5c4f] focus:ring-2 focus:ring-[#0f5c4f]/10";

export function EnrollmentStation({ schoolId, applications, academic, onDone }: { schoolId: number; applications: any[]; academic: any; onDone: () => void }) {
  const [applicationId, setApplicationId] = useState(""); const [classId, setClassId] = useState(""); const [sessionId, setSessionId] = useState(""); const [admissionNo, setAdmissionNo] = useState(""); const [admittedOn, setAdmittedOn] = useState(new Date().toISOString().slice(0, 10));
  const enroll = trpc.nsos.admissions.enrol.useMutation({ onSuccess: result => { const guardianMessage = result.guardianCreated ? "A new guardian record was created and linked." : "The submitted guardian record was linked."; const message = result.letterDelivery === "sent" ? `Enrollment confirmed. Biodata was transferred, ${guardianMessage.toLowerCase()} The admission letter was emailed to the recorded guardian.` : result.letterDelivery === "not_sent_no_guardian_email" ? `Enrollment confirmed and biodata transferred. ${guardianMessage} No guardian email was provided, so no admission letter was sent.` : `Enrollment confirmed and biodata transferred. ${guardianMessage} The admission letter could not be delivered; check the communication log and resend after correcting the email setup.`; result.letterDelivery === "failed" ? toast.warning(message) : toast.success(message); setApplicationId(""); setAdmissionNo(""); onDone(); }, onError: error => toast.error(error.message) });
  const accepted = applications.filter(item => item.status === "accepted");
  return <section className="rounded-[1.2rem] border border-[#e0e5df] bg-white p-5 shadow-[0_10px_32px_rgba(16,45,35,0.035)] sm:p-6"><div className="flex items-start gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#e2f1e8] text-[#176145]"><BadgeCheck className="h-4 w-4" /></span><div><p className="text-sm font-semibold text-[#2d4439]">Enrollment confirmation</p><p className="mt-1 text-xs text-[#758079]">Create the learner profile from approved admission biodata, confirm the first class assignment, and email a school-branded admission letter to the recorded guardian.</p></div></div><form onSubmit={(event: FormEvent) => { event.preventDefault(); enroll.mutate({ schoolId, applicationId: Number(applicationId), classId: Number(classId), sessionId: Number(sessionId), admissionNo, admittedOn }); }} className="mt-5 grid gap-3 sm:grid-cols-5"><label className="grid gap-1.5 text-xs font-semibold text-[#43534c]"><span>Accepted applicant</span><select required className={inputClass} value={applicationId} onChange={event => setApplicationId(event.target.value)}><option value="">Select applicant</option>{accepted.map(item => <option key={item.id} value={item.id}>{item.applicationNo} · {item.firstName} {item.lastName}</option>)}</select></label><label className="grid gap-1.5 text-xs font-semibold text-[#43534c]"><span>Admission number</span><input required className={inputClass} value={admissionNo} onChange={event => setAdmissionNo(event.target.value.toUpperCase())} placeholder="NSOS/2026/001" /></label><label className="grid gap-1.5 text-xs font-semibold text-[#43534c]"><span>First class</span><select required className={inputClass} value={classId} onChange={event => setClassId(event.target.value)}><option value="">Select class</option>{(academic?.classes ?? []).map((item: any) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="grid gap-1.5 text-xs font-semibold text-[#43534c]"><span>Session</span><select required className={inputClass} value={sessionId} onChange={event => setSessionId(event.target.value)}><option value="">Select session</option>{(academic?.sessions ?? []).map((item: any) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label className="grid gap-1.5 text-xs font-semibold text-[#43534c]"><span>Admission date</span><input required type="date" className={inputClass} value={admittedOn} onChange={event => setAdmittedOn(event.target.value)} /></label><button disabled={!accepted.length || enroll.isPending} className="sm:col-span-5 w-fit rounded-xl bg-[#0f5c4f] px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50">{enroll.isPending ? "Confirming…" : "Enroll & send admission letter"}</button></form>{!accepted.length ? <p className="mt-3 text-xs text-[#758079]">Accepted applications will be ready to enrol here.</p> : <p className="mt-3 text-xs leading-5 text-[#758079]">Enrollment is the final school approval. It never sends a letter until the learner profile and class assignment have been confirmed.</p>}</section>;
}
