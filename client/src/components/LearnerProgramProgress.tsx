import { BookOpenCheck, CalendarDays, CircleCheck } from "lucide-react";

type LearnerMilestone = { id: number; title: string; sortOrder: number; moduleTitle?: string | null; pathwayTitle?: string | null; pathwayType?: string | null; status: string; reviewedAt: Date | string | null; note: string | null };
type LearnerProgramme = { id: number; status: string; enrolledOn: Date | string; completedAt: Date | string | null; programTitle: string; deliveryMode: string | null; cohortName: string | null; attendance: { total: number; attended: number; lastRecordedOn: Date | string | null }; milestones?: LearnerMilestone[]; milestoneSummary?: { total: number; reviewedComplete: number; inProgress: number } };

export function LearnerProgramProgress({ items }: { items: LearnerProgramme[] }) {
  if (!items.length) return <section className="rounded-[1.2rem] border border-[#e0e5df] bg-white p-5 shadow-[0_10px_32px_rgba(16,45,35,.035)]"><div className="flex gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#edf3ed] text-[#176145]"><BookOpenCheck className="h-4 w-4" /></span><div><p className="text-sm font-bold text-[#2d4439]">My programmes</p><p className="mt-1 text-[11px] leading-5 text-[#758079]">Your active training programmes and progress will appear here when your learning organisation has enrolled you.</p></div></div></section>;

  return <section className="rounded-[1.2rem] border border-[#d5e6d9] bg-white p-5 shadow-[0_10px_32px_rgba(16,45,35,.035)]"><div className="flex gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#edf3ed] text-[#176145]"><BookOpenCheck className="h-4 w-4" /></span><div><p className="text-sm font-bold text-[#2d4439]">My programmes</p><p className="mt-1 text-[11px] leading-5 text-[#758079]">This view shows only your own enrolments and reviewed curriculum progress. It does not create a certificate or verify a credential.</p></div></div><div className="mt-4 grid gap-3">{items.map(item => { const rate = item.attendance.total ? Math.round((item.attendance.attended / item.attendance.total) * 100) : null; return <article key={item.id} className="rounded-xl border border-[#e3e9e4] bg-[#fbfcfa] p-4"><div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex flex-wrap items-center gap-2"><p className="text-sm font-bold text-[#30463b]">{item.programTitle}</p><span className={`rounded-full px-2 py-1 text-[10px] font-bold capitalize ${item.status === "completed" || item.status === "active" ? "bg-[#e6f4e9] text-[#176145]" : "bg-[#fff4df] text-[#855d15]"}`}>{item.status}</span></div><p className="mt-1 text-[11px] text-[#718078]">{item.deliveryMode?.replaceAll("_", " ") ?? "Delivery mode not set"}{item.cohortName ? ` · ${item.cohortName}` : ""}</p></div>{item.status === "completed" && <span className="inline-flex items-center gap-1 text-[10px] font-bold text-[#176145]"><CircleCheck className="h-3.5 w-3.5" />Completion recorded</span>}</div><div className="mt-3 grid gap-2 text-[11px] text-[#60736a] sm:grid-cols-2"><span className="inline-flex items-center gap-1.5"><CalendarDays className="h-3.5 w-3.5 text-[#39755d]" />Enrolled {new Date(item.enrolledOn).toLocaleDateString("en-NG")}</span><span>Participation: {rate === null ? "No attendance recorded" : `${rate}% across ${item.attendance.total} records`}</span></div>{item.milestones?.length ? <div className="mt-4 border-t border-[#e4ebe5] pt-3"><div className="flex flex-wrap items-center justify-between gap-2"><p className="text-[11px] font-bold text-[#385044]">Learning milestones</p><span className="text-[10px] text-[#61766b]">{item.milestoneSummary?.reviewedComplete ?? 0}/{item.milestoneSummary?.total ?? item.milestones.length} reviewed complete</span></div><div className="mt-2 grid gap-1.5">{item.milestones.map(milestone => <div key={milestone.id} className="flex items-center justify-between gap-3 rounded-lg border border-[#e5ece6] bg-white/80 px-2.5 py-2"><span className="min-w-0"><span className="block truncate text-[10px] font-semibold text-[#476056]">{milestone.sortOrder}. {milestone.title}</span>{(milestone.pathwayTitle || milestone.moduleTitle) && <span className="mt-0.5 block truncate text-[9px] text-[#708078]">{milestone.pathwayTitle ? `${milestone.pathwayTitle}${milestone.pathwayType ? ` · ${milestone.pathwayType.replaceAll("_", " ")}` : ""}` : ""}{milestone.pathwayTitle && milestone.moduleTitle ? " · " : ""}{milestone.moduleTitle ?? ""}</span>}</span><span className={`shrink-0 rounded-full px-2 py-1 text-[9px] font-bold ${milestone.status === "reviewed_complete" ? "bg-[#e6f4e9] text-[#176145]" : milestone.status === "in_progress" ? "bg-[#eff4fb] text-[#41688f]" : "bg-[#f0f2ef] text-[#718078]"}`}>{milestone.status.replaceAll("_", " ")}</span></div>)}</div><p className="mt-2 text-[9px] leading-4 text-[#718078]">Milestone status is recorded by your learning organisation after review. It is not a grade, certificate, or verified credential.</p></div> : <p className="mt-4 border-t border-[#e4ebe5] pt-3 text-[10px] leading-4 text-[#718078]">No active learning milestones have been published internally for this programme yet.</p>}</article>; })}</div></section>;
}
